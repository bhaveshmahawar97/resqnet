/**
 * BackgroundOrbs — Disabled
 * Previously rendered decorative animated orbs.
 * Removed as part of the UI maturity redesign (too futuristic/noisy).
 * Component kept as no-op to avoid breaking existing imports.
 */
export default function BackgroundOrbs() {
  return null;
}
